package utils

// Use named return values (sum, product)
// Do NOT explicitly return values (use bare return)
// No division, no floats, no tricks
// Then write one unit test that tests:
// Stats(3, 4)
// Expected: sum = 7, product = 12
// Constraints
// No table tests
// Standard library only
// Clean test name
// Code must compile and pass go test

// sum should become a + b

// product should become a * b

func Stats(a, b int) (sum int, product int) {
	sum = a + b
	product = a * b
	return

}
